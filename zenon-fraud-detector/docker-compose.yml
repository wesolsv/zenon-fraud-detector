services:
  db.zenon:
    image: mysql:9
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: senha123
      MYSQL_DATABASE: zenon
    ports:
      - '3306:3306'
    volumes:
      - mysql.zenon:/var/lib/mysql
volumes:
  mysql.zenon: